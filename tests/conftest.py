"""Root test config.

Per-framework sys.path setup lives in `tests/frameworks/<name>/conftest.py`
so that flat module names (agent, output_models, ...) don't collide across
frameworks. See those files for details.
"""
